<h1>Latest News Stories</h1>

    <% @news.each do |news| %>
 <div class="row news-index">
     <div class="col-md-3 news-img-index"> <%= image_tag news.avatar.url(:thumb)%>  </div> 
      <div class="col-md-9 news-teaser"><h3>  <%= link_to news.title, news %> </h3>
		 <small class="text-muted"><%= news.updated_at.strftime("%B %d %Y")%></small> 
    <p><%= news.leader %> <small><%=     
    link_to "read more", news 
 %></small><br/>
</p>
 
      </div>
		
		
		  </div>
		  
		 
		  
		  
    <% end %>
	<div class="pagination">
  <%= will_paginate @news %>
</div>
<br>

<% content_for :rh do %>

		<% if current_user %><div class="edit alert" >
		 <i> <%= link_to ' Add a Story  ', new_news_path, class: "admin-edit fa fa-user-plus", dismissible: true %> </i>
 
		 <button class="close" data-dismiss="alert" type="button">
		     <span aria-hidden="true">×</span><span class="sr-only">Close</span>
		   </button>
   
		 </div>
		
<% end %>
		 <%= render '/layouts/ltd.html.erb'%>
		 <%= render '/layouts/tenets.html.erb'%>
<% end %>

