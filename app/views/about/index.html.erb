


<h1><%= @content.title %></h1>


<div class="about"><%= simple_format @content.content %></div>

 	<% if current_user %>
 	<div class="edit alert" >
 <i> <%= link_to ' Edit ', edit_page_path(@content), class: "admin-edit fa fa-edit", dismissible: true %> </i>|<i> <%= link_to ' Delete', @content, method: :delete, data: { confirm: 'Are you sure?' }, class: "admin-edit fa fa-trash-o" %></i>
 
 <button class="close" data-dismiss="alert" type="button">
     <span aria-hidden="true">×</span><span class="sr-only">Close</span>
   </button>
   
 </div>
 <% end %>

<% content_for :rh do %>
<%= image_tag 'mattwilliams.jpg', class: "img-fluid img-bio"%>
<% end %>