



 	<% if current_user %>
 	<div class="edit alert" >
 <i> <%= link_to ' Edit ', edit_event_path(@event), class: "admin-edit fa fa-edit", dismissible: true %> </i>|<i> <%= link_to ' Delete', @event, method: :delete, data: { confirm: 'Are you sure?' }, class: "admin-edit fa fa-trash" %></i>
 
 <button class="close" data-dismiss="alert" type="button">
     <span aria-hidden="true">×</span><span class="sr-only">Close</span>
   </button>
   
 </div>
 <% end %>
<div class="event-details">
<h2> <%= @event.start_time.strftime("%B %d - %I:%M %p") %></h2>
<h3>


 <%= @event.location %>
</h3>

<% unless @event.avatar.blank?%>
<div class="event-img">
  <%= image_tag @event.avatar.url(:medium), class: "img-fluid" %>
</div>
<% end %>
<p>
 
  <%= simple_format(@event.content) %>
</p>

</div>

<% content_for :rh do %>
<%= render '/layouts/upcoming_events.html.erb' %>
<% end %>
